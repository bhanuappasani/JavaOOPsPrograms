package interfacePackage;

public interface Interface_Without_Method {

	/* All the data members in Interface are public, static and final by default
	 * Java compiler adds adds public, static and final keywords before data members
	 * int number; String name; double decimal;
	 */
	int number = 5;
	String name = "I am Interface String member";
	double decimal = 2.4;
}
